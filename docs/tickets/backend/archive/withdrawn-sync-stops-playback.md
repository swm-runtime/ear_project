# [BE] 회수해도 재생 중인 사용자는 끝까지 듣는다 — 회수 동기화가 없다

| 항목 | 값 |
|---|---|
| 대상 | `GET /contents/withdrawn`(미구현) · `content-detail.controller.ts` 주석 · 클라이언트 재생 중단 연결 |
| 요청 파트 | 백엔드 (클라이언트 연결은 FE 별건) |
| 발행 날짜 | 2026-09-08 |
| 발견 시점 | `share-app-links` 완료 조건(회수된 콘텐츠 링크)을 실기기로 확인하다 — 재생 중 회수했더니 **5분이 지나도 계속 재생됐다** |
| 근거 문서 | `features/partner-control.md` 4.3(회수 — 노출면별 반영) · `features/admin.md` 4.4 |
| 심각도 | **중** — 회수(재생 미중단)와 재발행(위치 되살아남) 두 사례가 확인됐다. 데이터 손상은 아니지만 "즉시 반영"이라는 계약이 지켜지지 않는다 |
| 상태 | 대기 |
| 연관 | `tickets/frontend/pending/share-app-links-and-deep-link-routing.md`(같은 확인에서 발견) |

## 문제

`partner-control.md` 4.3은 회수의 반영 시점을 노출면별로 못박는다.

| 노출면 | 명세 | 실제(2026-09-08 실기기) |
|---|---|---|
| 카탈로그·탐색·검색 | 즉시 | ✅ |
| 이미 적립된 라이브러리 | 즉시 서버에서 삭제 | ✅ 사라진다 |
| 상세 조회 | 차단 | ✅ 진입 안 된다 |
| **재생 중인 세션** | **"오디오 서명 URL 발급 차단. 진행 중 재생은 버퍼 소진 시 중단"** | ❌ **5분이 지나도 끝까지 재생된다** |

서명 URL 만료는 5분인데(`audio_access_logs.expires_at` 실측) 그보다 오래 재생됐다.
**클라이언트가 파일을 통째로 버퍼링했기 때문**으로 보인다 — 15분짜리 mp3는 재생 초반에 전부 받아둘 수 있다.

명세의 "버퍼 소진 시 중단"은 **버퍼가 곧 파일 전체인 경우를 상정하지 않았다.** 문자 그대로는 위반이
아니지만, 이 조항의 목적(회수 즉시 청취 중단)은 달성되지 않는다.

## 왜 이렇게 됐나 — 동기화 채널이 없다

`partner-control.md` 4.3의 처리 순서 5번은 **"오프라인 회수 목록에 추가 → 클라이언트가 앱 실행·
포그라운드 복귀 시 동기화"** 이고, 조회 수단으로 `GET /contents/withdrawn?since=<timestamp>` 를
지정한다(같은 절 "클라이언트 동기화").

**그 엔드포인트가 없다.** 서버 코드가 스스로 그렇게 적고 있다.

```ts
// backend/src/modules/content-detail/content-detail.controller.ts:24
// `GET /contents/withdrawn`(회수 동기화 — partner-control 소유, 미구현)을 나중에 만들 때는 …
```

클라이언트에도 회수 목록을 받아 처리하는 코드가 없다(`grep withdrawn frontend/src` — 토스트 카피와
플레이어 상태 enum 뿐).

## 요청 내용

1. **`GET /contents/withdrawn?since=<ISO8601>` 를 구현한다.** `contents` 에서 `status = withdrawn`
   이고 `withdrawn_at > since` 인 `content_id` 목록을 돌려준다(별도 테이블 없이 — `domain.md` 14장).
2. **응답 계약을 `spec/api/` 에 등재한다.** 지금은 `partner-control.md` 본문에만 언급돼 있어 확정
   계약이 아니다. 어느 화면 문서에도 속하지 않으므로 위치 결정이 필요하다.
3. **재생 중단의 판정 주체를 정한다.** 클라이언트가 목록을 받아 스스로 멈추는 것 외에,
   진행 중 세션을 서버가 끊을 수단이 있는지(예: 진행 저장 4.3 응답에 회수 신호를 실어 보내기)를
   함께 검토한다 — **클라이언트는 이미 4.3 으로 주기적으로 서버와 통신한다.** 그쪽이 즉시성이 높다.
4. **오프라인 저장분(P1)** 은 이 티켓 범위 밖이다(`offline-download.md` 이연).

## 같은 뿌리의 두 번째 사례 — 재발행 (2026-09-08 실증)

회수뿐 아니라 **재발행에서도 같은 구멍이 드러났다.**

| 시각 | 일 |
|---|---|
| `03:45:43` | 재발행 — 서버가 `playback_progresses` **5행 삭제**(안 A) |
| `03:47:17`·`03:47:46` | 앱이 오디오 URL **재발급**(`refreshAudioUrl()` — 만료 전 자동 갱신) |
| `03:48:18` | 앱이 **169초를 새로 저장** ← 서버가 지운 것을 앱이 되살렸다 |

`refreshAudioUrl()` 은 URL 만 갈아끼우고 **세션 위치를 유지한다** — 그 자체는 옳다(갱신 시 화면이
변하지 않아야 한다, `player-uiux.md` 4.9). 문제는 **그 시점에 콘텐츠가 바뀌었다는 사실을 앱이
알 방법이 없다는 것**이다.

**안 A 는 "다음 진입"만 고친다.** 앱을 완전히 종료한 뒤 재진입하면 0초부터 시작한다(확인됨).
하지만 재발행 순간 듣고 있던 사용자는 낡은 위치를 그대로 들고 가고, 곧 서버에 다시 쓴다.

> **회수(재생이 안 멈춤)와 재발행(위치가 되살아남)은 다른 증상이지만 원인이 하나다** —
> 진행 중 세션에 서버 상태 변경을 전달하는 채널이 없다. 한 곳에서 함께 해결한다.

### 이 사례가 요청 3에 주는 근거

요청 3에서 "진행 저장(4.3) 응답에 신호를 싣는 안"을 후보로 적었는데, **이 사례가 그 안의 근거다.**
앱은 재발행 직후에도 4.3 으로 계속 서버와 통신했다(그래서 169초가 저장됐다). 그 응답에 회수·재발행
신호를 실었다면 **같은 왕복에서** 처리할 수 있었다.

## 완료 조건

- Given `GET /contents/withdrawn?since=<시각>` / When 그 시각 이후 회수된 콘텐츠가 있다 / Then 그 `content_id` 목록이 반환된다
- Given 앱이 그 콘텐츠를 재생 중이다 / When 관리자가 회수한다 / Then **버퍼가 남아 있어도 재생이 멈추고** "제공이 종료된 콘텐츠예요"가 안내된다
- Given 앱이 그 콘텐츠를 재생 중이다 / When 관리자가 **재발행**한다 / Then 앱이 **낡은 위치를 다시 저장하지 않는다**(서버가 지운 행이 되살아나지 않는다)
- Given 회수 시점에 재생 중이 아니었다 / When 앱을 포그라운드로 복귀시킨다 / Then 라이브러리·목록에서 그 콘텐츠가 사라져 있다(현재도 조회 시점에 반영되므로 회귀 확인)
- Given 계약 / When `spec/api/` 를 연다 / Then 이 엔드포인트가 등재돼 있다

## 참고 — 함께 확인된 정상 동작

같은 실기기 확인에서 아래는 **명세대로 동작했다.** 회귀 판단의 기준선으로 남긴다.

- 회수 즉시 `library_items` 가 삭제된다
- **복구해도 라이브러리는 되살아나지 않는다**(`partner-control.md` 4.3 "삭제된 `library_items` 는 복구하지 않는다")
- 상세 진입이 차단된다

## 진행 기록 (2026-09-08 — 서버 몫 구현 완료)

`feat(be)/new-ticket-batch`에서 요청 1·3을 구현했다.

- **요청 1** — `GET /contents/withdrawn?since=` 구현. 라우트는 `ContentDetailController` 안
  `:contentId` **위에** 선언해 등록 순서를 컨트롤러 선언 순서로 보장했다(모듈 초기화 순서에
  묶이지 않게). 실서버 부팅 실측: 'withdrawn'이 파라미터 라우트보다 먼저 매칭(200), since
  필터·형식 오류 400 확인.
- **요청 3 결정** — **위치 저장(4.3) 응답에 `content_status`를 싣는 안 채택.** 재생 중 이미
  주기적으로 도는 왕복이라 지연 상한이 저장 주기로 줄어든다. 재발행 감지는 기존
  `content_version` 필드로 충분(재발행 사례의 실제 구멍은 FE `refreshAudioUrl()`이 새 버전을
  조용히 받아들인 것 — FE 수정 필요). 회수 콘텐츠 저장 시 `content_status: "withdrawn"` 실측.
- **요청 2** — 계약 등재는 `changes/pending/player-api-withdrawn-sync.md` 발행(등재 위치
  제안: player-api.md).

**남은 것**: 완료 조건 2·3은 **FE 연결**이다 — ① 4.3 응답 `content_status=withdrawn` 시 재생
중단+안내 ② 포그라운드 복귀 시 회수 목록 동기화 ③ `refreshAudioUrl()`이 버전 변경을 감지하면
로컬 위치 폐기. FE 티켓 발행 필요(요청 시 작성).

## 진행 기록 (2026-09-08 — 계약 반영 완료, FE 연결만 남음)

BE 담당 지시로 계약을 당일 반영했다 — `player-api.md` 4.6 신설·4.3 `content_status` 추가,
`partner-control.md` 4.3 현행화(미결 "강제 중단 여부"도 확정 처리). 완료 조건 ①·④·⑤ 충족.
**남은 것은 조건 ②(재생 중단)·③(낡은 위치 재저장 방지) — FE 연결이며
`tickets/frontend/pending/withdrawn-republish-playback-sync.md`로 발행했다.** 그 티켓이 닫히고
실기기 재확인이 되면 이 티켓을 archive로 옮긴다.

## 처리 기록 (2026-09-08 — FE 연결 완료로 티켓 종료)

| 항목 | 값 |
|---|---|
| 발행 날짜 | 2026-09-08 |
| 반영 날짜 | 2026-09-08 |

남아 있던 완료 조건 ②(재생 중단)·③(낡은 위치 재저장 방지)이 FE 티켓
`withdrawn-republish-playback-sync`로 닫혔다. 실기기에서 iOS·Android 양쪽 확인 완료.

- 재생 중 회수 → **저장 주기(5초) 안에 중단**. 버퍼가 파일 전체여도 닿는다 —
  이 티켓이 지적한 "버퍼 소진 시 중단"의 구멍이 실제로 막혔다
- 재생 중 재발행 → 낡은 위치가 서버에 다시 저장되지 않는다
- 회수 목록 동기화가 앱 실행·로그인·포그라운드 복귀마다 돈다

요청 3에서 후보로 적었던 **"진행 저장(4.3) 응답에 신호를 싣는 안"이 채택됐고, 실제로
그 안이 주 채널로 동작한다.** `GET /contents/withdrawn`은 재생 중이 아니었던 세션의
보완 채널로 남는다.
