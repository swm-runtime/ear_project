# 알림 사전 안내 — 되짚기 팝업 폐지 · 버튼 가로 배치 (문서 반영 요청)

| 항목 | 값 |
|---|---|
| 발행 날짜 | 2026-09-06 |
| 발행자 | FE |
| 대상 문서 | `features/notification.md` 4.1 · `spec/uiux/onboarding-uiux.md` 4.8(되짚기 팝업 O11 잔재) |
| 관련 코드 | `frontend/src/features/notification/components/NotificationPrePromptModal.tsx` · `ReconsiderDialog.tsx`(삭제) |

## 확정 내용

1. **[나중에]는 되묻지 않고 바로 닫는다.** 되짚기 팝업(ReconsiderDialog)을 폐지하고 컴포넌트·카피를 함께 지웠다.
2. **사전 안내의 두 버튼을 가로로 둔다** — [나중에] 왼쪽(면·테두리 없음), [알림 받기] 오른쪽(면 채움). 동등 비중.
3. 다이얼로그·바텀시트의 모서리를 `radius.xl`(24)로 올렸다. 토큰을 새로 추가해 8곳을 한 번에 맞췄다.

## 왜 폐지했나

되짚기는 전환율을 올리지만 **거절을 한 번 무르는 상호작용**이다. 한 번으로 제한해도 "닫으려는데 또 뜬다"는 인상이 남는다. 우리에게는 **거절해도 되돌릴 수 있는 구조가 이미 있다** — [나중에]는 OS 다이얼로그를 띄우지 않으므로 권한이 "미결정"으로 남고, 설정의 알림 유도 배너((b)안, `notification.md` 4.1)가 회수 경로를 맡는다. 팝업을 한 번 더 띄워 얻는 것보다 잃는 인상이 크다고 판단했다.

**OS 다이얼로그를 [알림 받기]에서만 띄우는 규칙은 그대로다.** 그것이 이 화면의 존재 이유다.

## 수정 요청

- `notification.md` 4.1에서 되짚기 팝업 서술을 제거하고, [나중에]가 곧바로 닫는다는 것과 회수 경로가 설정 배너라는 점만 남겨 달라.
- `onboarding-uiux.md` 4.8에 O11(되짚기) 잔재가 있으면 함께 정리해 달라.
- 사전 안내의 버튼 배치(가로·순서·[나중에]는 면 없음)를 uiux에 반영해 달라.

## 완료 조건 (Given/When/Then)

- Given `notification.md` 4.1을 열었을 때, When 반영이 끝나면, Then 되짚기 팝업 서술이 없고 [나중에]가 즉시 닫는다고 적혀 있다.
- Given 사전 안내 화면에서, When [나중에]를 누르면, Then 추가 팝업 없이 닫히고 OS 권한은 미결정으로 남는다.
- Given 권한이 미결정인 사용자가 설정에 들어갔을 때, When 알림 섹션을 보면, Then 유도 배너가 노출된다(기존 규칙 불변).

---

## 처리 기록

| 항목 | 값 |
|---|---|
| 반영 날짜 | 2026-09-07 |
| 반영 문서 | `features/notification.md` 4.1·카피 표·완료 조건 · `features/onboarding.md` 4장 [알림]·상태 표·예외·결정 표·완료 조건 · `spec/uiux/onboarding-uiux.md` 화면 목록·흐름도·4.7·**4.8 삭제**·5장·8장·9장 |

### 반영 범위가 요청보다 넓었다

요청 대상은 `notification.md` 4.1과 `onboarding-uiux.md` 4.8이었는데, **O11(재고 팝업)이
`onboarding.md`에도 규칙 본문·상태 표·예외 처리·결정 표·완료 조건 5곳으로 퍼져 있었다.**
한 곳만 지우면 문서끼리 어긋나므로 함께 정리했다.

- `notification.md` 4.1 — 재고 팝업 서술 제거. **여는 시점**을 새로 명시했다(코치마크 종료 후,
  건너뛰기 포함 / 코치마크 없는 진입은 즉시). 완료 조건 2줄을 4줄로 교체.
- `onboarding.md` — 4장 [알림]의 재고 팝업 블록(수치 `N`·출처 규칙 포함) 전체 교체. 상태 표에서
  재고 팝업 행 삭제, 사전 안내 행에 버튼 배치 추가. 예외 처리 2줄 → 1줄. 결정 표 2행 → 1행.
  완료 조건 4개 → 3개.
- `onboarding-uiux.md` — 화면 목록 O11 행 삭제, **흐름도에서 O11 분기 제거**, 인디케이터 경계
  문장 갱신, 4.7의 "[나중에]는 약한 스타일" 규칙을 **가로·동등 비중**으로 뒤집음, **4.8 절 통째
  삭제**, 5장 공통 컴포넌트 표에서 `다이얼로그 | 되짚기` 행 삭제, 8장 금지 목록 5줄 교체,
  9장 미결에서 "재고 팝업의 수치와 출처" 삭제, 접근성의 O11 포커스 트랩 규칙 삭제.

### 뒤집힌 규칙 하나 — 기록해 둔다

기존 4.7은 **"[나중에]는 주 액션보다 약한 스타일로 두되 감추지 않는다. 여기서 두 버튼의 비중이
다른 것은 허용된다 — 되짚을 기회(O11)가 한 번 더 남아 있기 때문이다"**였다.

되짚기가 사라졌으므로 **그 허용의 근거가 사라졌다.** 이제 O10이 유일한 거절 지점이라
동등 비중이 필수다. 요청의 "[나중에] 왼쪽·면 없음, [알림 받기] 오른쪽·면 채움"과 일치한다.

### 남긴 것

- **와이어프레임 `onboarding.html`의 O11 화면은 지우지 않았다.** 시각 참조 동기화는 9장 미결
  목록이 관리하며, 그 항목에 "O11 화면은 지운다"를 명시해 뒀다.
- 다이얼로그·바텀시트 모서리 `radius.xl`(24)는 **디자인 토큰 변경**이라 uiux 문서가 소유하지
  않는다(`wireframe/style.css` 토큰 값은 임시값으로 명시돼 있다). 별도 반영 대상이 아니다.

완료 조건 3개 모두 충족.
