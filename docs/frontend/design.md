# 이어 디자인 원칙 — 애플 HIG 기준

> 2026-09-23 발행. 화면·컴포넌트를 만들거나 고칠 때 **이 문서를 먼저 본다.** 근거는 애플 Human Interface Guidelines(HIG)이고, 이 앱에서 확정한 값·이유를 적는다. 값의 원본은 `frontend/src/shared/theme/`(`index.ts` 토큰 · `motion.ts` 곡선)이다 — 여기 적힌 숫자와 코드가 다르면 코드가 틀린 것이다.
>
> 동작 규칙(`docs/features/`)·화면 상태·카피(`docs/spec/uiux/`)는 그대로 그쪽이 기준이다. 이 문서는 **어떻게 보이고 움직이는가**만 정한다.

## 0. 한 줄 원칙

**iOS 사용자가 "시스템 앱 같다"고 느끼게 만든다.** 브랜드는 검정 선 하나(로고)뿐이라 앱은 모노톤이고, 개성은 색이 아니라 **재질·곡률·움직임**으로 낸다. 애플이 정해 둔 것은 애플 값을 쓰고, 애플이 안 정한 것만 우리가 정한다.

HIG 원문: https://developer.apple.com/design/human-interface-guidelines/

---

## 1. 색 (HIG: Color)

| 역할 | 토큰 | 값 | 비고 |
|---|---|---|---|
| 바탕 | `color.background` | `#FFFFFF` | 앱의 유일한 큰 면. 어두운 건 플레이어뿐(`player.theme.ts`) |
| 면 | `color.surface` | `#F5F5F7` | 카드·시트 안 버튼(취소)·입력 상자 |
| 글자 | `color.textPrimary` / `textSecondary` | `#1A1A1E` / `#6E6E76` | |
| 강조 | `color.primary` | `#000000` | 버튼·선택. **유채색 강조는 두지 않는다** |
| 진행률 채움 | `color.progress` | `#8E8E93` | iOS systemGray. 사진 위에 놓이는 바 — 어두운 띠·흰 배경 양쪽에서 갈리는 중간값(2026-09-22 후보 6개 비교 후 확정) |
| 주의 | `color.danger` / `warning` | `#E5484D` / `#8A5B00` | 의미색은 강조가 아니다 — 소진·실패·미완에만 |

- **색만으로 상태를 구분하지 않는다**(HIG Accessibility) — 형태(바의 길이, 알약, 아이콘 채움)나 낭독기 라벨을 함께 둔다. 예: 완청은 "꽉 찬 바 + '완청한 콘텐츠' 라벨".
- 새 색을 넣고 싶으면 먼저 **"이게 앱의 유일한 유채색이 될 만큼 중요한가"** 를 묻는다. 아니면 회색 단계로 푼다.

## 2. 모서리 (HIG: Layout — corner concentricity)

- **모든 둥근 모서리는 연속 곡률**(`borderCurve: 'continuous'`, iOS 만 적용·Android 는 원호). 원호는 곡률이 변 중간에서 갑자기 시작해 각져 보인다. 썸네일·카드·시트·다이얼로그·버튼·검색 상자 전부.
- 반지름 토큰: `radius.sm 8` · `md 12` · `lg 16` · `xl 24` · `full 999`.

| 대상 | 반지름 |
|---|---|
| 썸네일(격자 타일·대표 카드) | `lg` 16 / 대표 카드 안 커버는 카드가 클립 |
| 작은 썸네일(미니플레이어·시트·상세 72) | `sm` 8 |
| 버튼·검색 상자·문단 | `md` 12 |
| 다이얼로그·바텀시트(위 모서리) | `xl` 24 — **면이 클수록 반지름을 키운다**(같은 부드러움으로 읽히려면) |
| 미니플레이어 카드 | 22 (`MINI_CARD_RADIUS`) — 캡슐 탭 바(높이 60)와 같은 결 |
| 캡슐(탭 바·세그먼트·알약 버튼) | `full` |

- **동심원 규칙**: 안쪽 모서리 반지름 = 바깥 반지름 − 안쪽 여백. 캡슐 안 선택 알약(inset 4)이 `full` 인 것은 둘 다 반원이라 성립한다.
- 이미지에 라운드를 직접 주지 않는다 — expo-image 는 `borderCurve` 를 모른다. **프레임 View 가 라운드·클립**한다.

## 3. 재질 (HIG: Materials · Liquid Glass)

- **떠 있는 면은 유리다** — 미니플레이어 카드·탭 바 캡슐. `shared/ui/GlassSurface`: iOS 26+ 리퀴드 글라스(`expo-glass-effect`, regular · light), 그 밑 iOS·Android 는 블러 60 + 흰 틴트 72%(`expo-blur`). runtime 5 부터.
- 유리는 **밑에 지나가는 콘텐츠가 있어야** 유리다. 그래서 탭 바·미니플레이어는 목록 위에 절대 배치하고, 목록은 `useBottomDockInset()`(캡슐 + 카드 + 간격 8)만큼 바닥 여백을 둔다.
- **유리 위에 유리를 올리지 않는다**(WWDC25 Meet Liquid Glass — "Always avoid glass on glass … use fills, transparency, and vibrancy for the top elements"). 유리는 유리를 샘플링하지 않아 두 번째 유리는 밑의 유리를 건너뛰고 콘텐츠만 비춘다. **탭 바의 선택 알약은 유리가 아니라 캡슐 유리 위의 검정 8% 채움**이다(`GlassPill.lens`, 2026-09-24 저녁). 09-23~24 의 clear 렌즈(어두운 썸네일 위에서 검은 구멍)·regular 렌즈(회색 판)는 전부 이 규칙을 어긴 결과였다. 세그먼트의 알약은 바탕이 유리가 아니라 clear 유리 렌즈 그대로.
- **알약이 눌려 캡슐 밖으로 넘칠 때**(탭 바 1.3×) 채움은 캡슐 자리 안에만 그린다 — 캡슐의 유리·경계가 채움 아래로 그대로 비치고, 캡슐 밖으로 나간 부분은 **완전히 투명**하다(2026-09-24 PM). 구현: `GlassPill.lens` 에 캡슐 자리를 알약 로컬 좌표의 역변환(translate·1/scale)으로 준다. 떠오름은 림(흰 하이라이트 + 그림자)과 아이콘 확대가 보인다.
- 유리 위 경계는 hairline 윤곽(검정 10%) + 은은한 그림자(`0 4px 20px rgba(0,0,0,.10)`). 불투명 색을 유리 위에 깔면 유리가 사라진다 — 컨테이너는 `transparent`.
- 사진 위 표식(더보기 점·"새로운 주제" 글자)은 **스티커(불투명 원·알약)를 쓰지 않는다.** 아래 변 검정 그라데이션(투명→75%, 56pt) 또는 SVG 드롭섀도 필터로 읽히게 한다.

## 4. 움직임 (HIG: Motion)

원본 `shared/theme/motion.ts`. **여기 값 외의 `Easing.*`·기본 스프링을 쓰지 않는다.**

| 이름 | 값 | 쓰는 곳 |
|---|---|---|
| `easing.easeInOut` | bezier(0.42, 0, 0.58, 1) | UIKit 기본. 길이가 정해진 대칭 전환 |
| `easing.easeOut` | bezier(0, 0, 0.58, 1) | 페이드·줄 이동·아이콘 회전·스와이프 퇴장 |
| `spring.smooth` | stiffness 195 · damping 28 · mass 1 (응답 ≈ 0.45s, 임계 감쇠) | 시트·패널처럼 큰 면 |
| `spring.snappy` | stiffness 440 · damping 42 · mass 1 (응답 ≈ 0.3s) | 세그먼트·탭 알약·시크 손잡이·복귀 |
| `spring.jelly` | stiffness 300 · damping 17 · mass 1 (감쇠비 ≈ 0.5, 한 번 넘쳤다 돌아온다) | **형태만** — 탭 알약의 부풀기·늘어남·복귀(iOS 26 버블). 위치에는 쓰지 않는다 |
| `duration.fast / normal` | 160 / 240ms | 짧은 전환 |

- **손이 관여하는 전환은 전부 스프링**이고, **놓는 순간의 속도를 `velocity` 로 넘긴다**(플레이어 시트). 던지면 빨리, 살살 놓으면 천천히. 끝점 overshoot 은 막는다(모션 레이어가 좌표 밖으로 튄다).
- **끌기는 1:1** — 시트가 손가락보다 빨리 달아나지 않는다.
- 선택 표시는 **하나가 미끄러진다**(세그먼트·탭 바 알약). 칸마다 켜고 끄지 않는다. 라벨 색은 알약이 지나가는 만큼 겹쳐(두 겹 텍스트 + 불투명도) 같은 프레임에 바뀐다.
- 플레이어 닫힘: 아트워크는 **시트에 실려 내려가다 미니플레이어 근처(진행 0.35 아래)에서만** 썸네일로 줄어든다(애플 뮤직). 시트 높이는 내려가는 동안 화면 높이 그대로(카드가 미끄러진다).
- **플레이어 아트워크는 재생 상태를 크기로 말한다**(애플 뮤직 Now Playing, 2026-09-25 PM): 정지하면 0.85 로 줄고 재생하면 `smooth` 스프링으로 제자리. 열림·닫힘 모션 중과 재생 목록이 열렸을 땐 1 고정(모션 레이어와 교차하는 순간 크기가 다르면 두 장으로 보인다). 미니→플레이어 전환 자체(시트가 미니 카드에서 자라고 아트워크가 썸네일에서 커지며 속도 계승 스프링, 아무 데나 끌어 닫기)는 애플과 같은 문법이라 유지 — 애플은 구현을 공개하지 않는다(DTS: "unable to share how the Music app achieved this").
- 레이아웃 속성(width·height·top)을 움직이는 모션은 JS 스레드다 — 그 동안 JS 를 잡는 일(fetch 응답 처리·0.5초 위치 틱 리렌더)을 미룬다. 가능하면 `transform`·`opacity` 로 짜서 네이티브 드라이버를 쓴다.
- `prefers-reduced-motion` 은 아직 미결(§8).

## 5. 컴포넌트 규칙

### 상단 — 투명 시스템 바 + 콘텐츠 안 큰 제목 줄 (HIG: Navigation bars, iOS 26) — 라이브러리·탐색·검색
**애플은 상단을 "떠 있는 컨트롤"이 아니라 "바"로 만든다**(PM 2026-09-25 23:50 "애플이라면 상단을 어떻게" → "ㄱㄱ"). 떠 있는 유리 검색창·흰 칩은 어두운 카드가 밑에 오면 글자가 죽었고(23:48 실기기), 바 밑 블러는 바가 있어야 시스템이 그린다. 그런데 **UIKit 큰 제목은 바 버튼과 같은 줄에 못 둔다**(iOS 26: 버튼은 위 줄, 큰 제목은 그 밑 — `.inline` 모드도 마찬가지) — 애플 뮤직·앱스토어·팟캐스트 탭 화면은 그래서 **제목 줄을 콘텐츠로 직접 그린다**(09-26 00:29 PM 애플 뮤직 검색 탭 스샷: 큰 제목 "검색" + 프로필 원이 같은 줄, 검색창은 그 밑 채움 필드, 스크롤하면 바에 작은 "검색"). iOS 26 시스템 탭 바 갈래(`HAS_NATIVE_TAB_BAR`)의 라이브러리·탐색·검색 탭은 이 문법이다:
- **바는 투명**(`NativeMainTabs` `TRANSPARENT_BAR`: `headerTransparent`, 제목 없음, 그림자 없음). react-native-screens 는 배경이 투명일 때만 시스템 모양(iOS 26 유리 + scroll edge 블러)을 남기고 아니면 불투명 면을 깐다(`configureWithOpaqueBackground`). 큰 제목 모드는 안 쓴다. 목록은 `contentInsetAdjustmentBehavior="automatic"` 으로 바 밑을 비우되 **제목 줄만 빈 바 줄(44)만큼 위로 올린다**(`useNativeBarPullStyle` — 첫 줄 marginTop −44. 뮤직처럼 큰 제목이 상태 바 바로 밑, 00:52 PM "위쪽에 공간 너무 많다". 인셋 자체를 줄이면(#733 contentInset −44) 바 밑 블러 영역도 줄어 작은 제목이 콘텐츠에 가린다 — 01:55 PM. 반대로 **블러를 바 줄보다 더 내리려면 인셋을 늘린다**: `useNativeBarBlurProps` contentInset top +`BAR_BLUR_EXTENSION` 96(02:12 24 → 02:18 "훨씬 내려"), 제목 줄은 그만큼 더 올린다), 스크롤 뷰가 아닌 상태 화면은 상태 바만 비운다(`useNativeHeaderInset`).
- **큰 제목 줄은 콘텐츠의 첫 줄**(`shared/ui/LargeTitleRow`: 34pt bold = `font.size.xxl`, 높이 52, 오른쪽 `trailing`). 라이브러리는 링 + 필터 툴바 캡슐, 탐색은 잔여 링, 검색은 없음 — **제목과 컨트롤이 같은 줄**(00:25 PM "높이 맞추자"). 목록과 같이 스크롤한다.
- **바의 작은 제목은 페이드인**(`useFadingNativeTitle`: 제목 줄이 바 밑으로 절반 들어갈 때 시작, 다 들어가면 1 — 네이티브 드라이버). 큰 제목이 줄어드는 UIKit 모션 대신 애플 뮤직의 동작.
- **검색 필드는 콘텐츠 안 채움**(`GlassCapsule variant="fill"` · `SearchInputRow variant="fill"` — full, 40). 유리는 떠 있는 컨트롤 층에만(3장). 라이브러리는 제목 줄 밑(받아 둔 목록을 그 자리에서 좁힌다), 탐색도 제목 줄 밑(누르면 검색 화면 E6 이 스택에 올라온다 — 큰 제목 "검색" + 채움 필드 + 취소, 제목·필드는 목록 밖 **고정**: 결과·로딩으로 목록이 바뀔 때 입력 상자가 내려가면 키보드가 떨어진다). 탭 바 오른쪽 **검색 탭**(`tabBarSystemItem: 'search'`, #730)은 뺐다 — 탐색 검색 필드와 입구가 둘이라(01:20 PM "탐색에 있는 검색은 어떻게 해").
- 주제 칩(탐색)·조건 요약·배너(라이브러리)는 제목 줄 밑 콘텐츠로 같이 스크롤. 고정하려면 바 *안*에 넣어야 하는데 공개 API 가 없다.
- 시도했다 버린 것(09-25 23:50 ~ 09-26 00:25, #730·#731): 시스템 큰 제목 + 바 오른쪽 아이템 + 바 안 시스템 검색창 — 링이 제목 위 줄에 혼자 떴다. `useNativeHeaderSearchBar` 도 그때 것으로 삭제.
아래 "떠 있는 머리 줄"은 JS 탭 바 갈래(iOS 26 미만·Android)의 규칙이다.

### 떠 있는 머리 줄 (HIG: Liquid Glass — 콘텐츠 위의 컨트롤 층)
라이브러리(검색 캡슐 · **잔여 링 + 필터를 묶은 툴바 캡슐**(`LibraryToolbar`, 09-25 PM "유리 조각이 셋" — iOS 26 이 툴바 버튼을 한 캡슐로 묶듯 유리는 줄에 둘만) · 조건 요약 · 배너)·탐색(검색 캡슐 · 잔여 링 유리 원 · 칩)의 머리 줄은 `shared/ui/FloatingHeader` 안에서 **배경 없이 목록 위에 떠 있다**(2026-09-24 PM "배경을 없애버리자"). 목록이 그 밑으로 흐르고 컨트롤은 각자 유리(검색 캡슐·잔여 링·세그먼트 트랙·필터 원). 머리 줄이 상태 바를 채우고 자식 높이를 올리면 목록이 `paddingTop`(`useFloatingHeaderInset`) 으로 비운다 — 시스템 탭 바(iOS 26)에서는 스크롤 뷰가 상태 바를 이미 비우므로 머리 줄 높이만. 새로고침 스피너는 `progressViewOffset` 으로 머리 줄 아래에 띄운다. **스크롤 맨 위에서는 컨트롤이 면(surface), 24pt 내리면 유리**(PM 2026-09-25 결정 — `useFloatingHeaderScroll` → `GlassCapsule` 덮개 불투명도, 네이티브 드라이버. 16:30 에 "그냥 상단 blur"(상태 바 얘기)를 잘못 읽고 걷어냈다가 17:25 "왜 뺐어"로 복구 — **PM 이 정한 것은 PM 말 없이 되돌리지 않는다**). **상태 바 밑 점진 블러는 iOS 26 시스템 scroll edge effect 다**(`UIScrollView.topEdgeEffect = .soft` — 애플 뮤직·설정과 같은 것). **로컬 네이티브 모듈 `modules/scroll-edge-effect`**(runtime 10, 2026-09-25 20:14 "blur 왜 뺐어")가 목록의 UIScrollView 에 직접 건다(`useSystemScrollEdgeEffect(listRef, mounted)` — 목록 마운트 뒤·탭 포커스마다). react-native-screens 의 자동 적용은 스택 화면 마운트 때 `subviews[0]` 을 한 번만 따라가 탭 바 컨트롤러 밑의 늦게 뜨는 목록엔 닿지 않았고, JS 재현(블러 띠 5·14장 → 계단, MaskedView 마스크 → UIVisualEffectView 가 마스크 미지원이라 얼룩)은 전부 폐기했다. 그 밑 iOS·Android 는 없다(애플도 없다). masked-view·linear-gradient 패키지는 rt 9 에 들어간 채 남아 있다.

### 탭 바 — 떠 있는 캡슐 (HIG: Tab bars, iOS 26)
**iOS 26 은 시스템 탭 바를 쓴다**(`app/navigation/NativeMainTabs`, `HAS_NATIVE_TAB_BAR` — 2026-09-24 밤 PM "애플이 기본으로 제공하는 애니메이션 없나"). `@react-navigation/bottom-tabs/unstable` 의 `createNativeBottomTabNavigator` = `UITabBarController`(react-native-screens BottomTabs). 선택 알약의 부풀기·끌기·굴절·색수차·끝 넘김 고무줄이 애플 코드 그대로고, 이건 비공개 API 라 흉내낼 수 없다(아래 캡슐의 형태 스프링은 그 밑 OS 용이다). 미니플레이어는 `bottomAccessory`(Music 앱 자리 — 탭 바 위) 로 들어가고 유리·폭·높이·모서리는 시스템이 준다. **액세서리는 항상 마운트하고, 볼 게 없으면 숨긴다**(`useIsMiniPlayerVisible` → `tabBarAccessoryHidden`, 09-25 23:03). 숨김·표시 전환은 시스템 애니메이션(`setBottomAccessory(nil/accessory, animated:)` — 탭 바로 거둬들이고 솟아나는 유리 병합, "닫을 때 물방울처럼"). 언마운트 방식은 RNS 가 내용을 먼저 떼 애니메이션이 멈췄고(22:29), 내용만 비우면 빈 유리 껍데기가 남았다(22:56). React Navigation 이 RNS 의 `bottomAccessoryHidden` 을 옵션으로 안 넘겨 **`patches/@react-navigation+bottom-tabs`**(patch-package, postinstall)로 `tabBarAccessoryHidden` 을 뚫었다 — 패키지를 올릴 때 패치가 깨지면 `npm run postinstall` 이 알려 준다. **미니플레이어는 닫을 수 없다**(09-25 23:24 — 애플 뮤직과 같다. 시스템 액세서리는 껍데기가 합쳐지는 퇴장 모션이 없어 내용만 사라지는 게 더 어색했다; `changes/pending/mini-player-not-dismissable.md`). 위로 끌어 열기만 남는다. 숨김은 볼 게 없을 때(세션·스냅샷 없음)만 — 내용은 높이를 채워 세로 가운데, 썸네일 36·행 위아래 4(카드의 40·6 보다 작게. 09-24 밤 "아래 공백이 크고 썸네일이 큼"), 진행바는 위 변. 아이콘은 SF Symbols(`books.vertical`·`safari`·`person`, 선택 시 `.fill`). 배경·알약 색은 iOS 26 에서 바꿀 공개 API 가 없다 — 시스템 값이 곧 0장 원칙이다. 목록 바닥 여백은 시스템 안전영역(탭 바 + 액세서리)을 `contentInsetAdjustmentBehavior="automatic"`(`DOCK_SCROLL_PROPS`)이 비운다. `tabBarMinimizeBehavior: none` — 최소화는 껐다(09-25 18:00: 줄어드는 유리 모핑 중간 프레임이 큰 아이콘 잔상으로 남고, screens 의 최소화 + 액세서리 + 모달 조합 버그 #4176 로 액세서리가 굳는다. 목록도 그만큼 길지 않다).

**그 밑 iOS·Android 는 JS 캡슐** — `app/navigation/CapsuleTabBar`. 높이 60, 칸 96×52 ×3, **캡슐 폭 296 = 칸 288 + 양옆 inset 4**(2026-09-24 — 유리를 288 로 그리면 끝 칸의 알약이 테두리에 붙는다; 위아래와 같은 4pt 를 양옆에도 둔다. 미니플레이어 카드 288 보다 8 넓다), **바닥 여백 = `max(안전영역 − 12, 16)`** — 홈 인디케이터 기기 22pt, 없는 기기 16pt. iOS 26 네이티브 탭 바의 자리(공식 수치 없음, 실측 재현들이 일치 — 인디케이터 바 위 ~9pt)와 같다(2026-09-24 PM "애플이 떨어져 있는 만큼". 09-23 "끝으로" 16 은 인디케이터에 걸쳤고, 09-24 오후의 34 는 너무 높았다). **선택 알약은 캡슐 유리 위의 검정 8% 채움**(`GlassPill` + `lens` — 유리가 아니다, 3장 "유리 위에 유리" 규칙). 아이콘 24 + 라벨 11(600) — 채움·색은 알약이 그 칸에 겹친 만큼 바뀐다(두 겹 + 불투명도). **알약은 잡고 끌 수 있다**(가로 4pt 넘게 움직이면 끌기, 놓으면 가까운 칸에 스냅 + 이동). **움직일 때 액체처럼 변한다**(2026-09-24 PM, 실기기와 비교하며 세 번 고침) — 애플의 알약은 누르면 **버블로 부풀고**(1.3×, jelly 오버슈트), **움직이는 동안은 줄어들어 둥근 방울**이 되고, 자리에 닿으면 **부풀며 출렁인다**(jelly bloom). 늘어나는 건 **끝을 넘겼을 때**(overdrag 고무줄)뿐 — 처음에 "진행 방향으로 늘어남"으로 만들었던 건 거꾸로였다("줄어드는 게 정상"). 우리 값: 끌기 수축 −15%(snappy 로 한 번) · 탭 이동 수축 −12% 90ms 뒤 jelly 로 0 · 끝 넘김 = 넘긴 거리/칸 폭 × 45% (수축 상쇄 후 최대 +35%) · 놓으면 jelly 로 0. 세로는 가로 변화의 ½ 이 반대로. 아이콘·라벨은 그대로. 위치(x)는 snappy — 위치가 튀면 칸을 넘는다. 차지 높이를 `BottomTabBarHeightCallbackContext` 로 보고한다.

### 미니플레이어 — 유리 카드
**탭 바 독(`CapsuleTabBar`) 안에 하나만 산다** — 캡슐과 같은 `GlassGroup`(spacing 28)이라 아래로 끌면 물방울처럼 합쳐진다(iOS 26). 탭 밖 화면은 `placement="floating"`. 캡슐 위 8, 폭 = 캡슐 폭(`theme.dock.width` 288) 가운데, 모서리 22. 행 54(6 + 썸네일 40 + 6 + **아래 변 진행바 2** — 09-25 PM: 위 변은 유리 림 하이라이트와 겹쳐 테두리가 두 겹으로 읽혔다. 양옆 12 안쪽, 끝 둥글게) · 썸네일 40(`sm` 8 연속 곡률) · 제목 14(600) · 카테고리 12(회색, 주제 앞 두 개 ` · `). 종료는 **아래로 끌어 캡슐에 흡수**(65pt 이동, scaleY→0.12 · scaleX→0.92 · 페이드, 40%/800dp·s 임계, snappy) — 왼쪽 스와이프는 없다(2026-09-23). 임계 미달 복귀 snappy.

### 시트·다이얼로그 (HIG: Sheets, Alerts)
위 모서리 `xl` + 연속 곡률. 버튼 `md` + 연속 곡률. 기본 동작은 검정 채움, 보조 동작은 `surface` 면(테두리 없음). 세 번째 선택지("오늘은 그만 보기" 류)는 버튼 줄에 세우지 않고 **제목 줄 오른쪽 위 작은 회색 글자**(xs, 밑줄 없음, hitSlop 으로 44pt). 탭 가능한 글자에 밑줄을 쓰지 않는다 — 웹 문법이다.

### 세그먼트 컨트롤 (HIG: Segmented controls)
**탐색 주간·월간·전체(`PopularPeriodToggle`)는 iOS 에서 시스템 `UISegmentedControl` 그대로다**(2026-09-26 02:19 PM "iOS 26 에서 쓰이는 기본 토글로") — 로컬 네이티브 모듈 `modules/system-segmented-control`(runtime 11)이 색·글꼴·모서리를 하나도 손대지 않고 노출한다. iOS 26 에서는 OS 가 그리는 유리 선택바(탭하면 선택 칸이 미끄러진다)이고, OS 가 바뀌면 같이 바뀐다. 크기는 Yoga 가 모르므로 호출부가 준다(높이 32 = iOS 기본, 칸 52×3). 선택 칸의 기준은 여전히 서버 응답 `selected` — 시스템은 탭 즉시 스스로 움직이므로, 비활성 중 탭·전환 실패로 `selected` 가 안 바뀌면 `key` 로 다시 그려 되돌린다(uiux explore 4.10). `enabled` 는 내리지 않는다(시스템이 흐리게 그려 전환마다 깜빡인다). JS 재현 세 가지(`glass`·`system`·`modern`)는 iOS 26 실기기에서 전부 "옛날 것 같다"는 평을 받았고, 시스템 컨트롤이 답이었다 — **시스템에 같은 부품이 있으면 흉내 내지 말고 그 부품을 쓴다**(탭 바를 iOS 26 시스템 탭 바로 바꾼 것과 같은 결론). Android·모듈 없는 빌드는 아래 `modern` 으로 내려간다.

`shared/ui/SegmentedControl`(JS) — 탐색 토글의 Android·폴백(칸 48×26)은 **`appearance="modern"` — iOS 26 앱들의 캡슐 선택바**(2026-09-25 23:30 PM "현대식 애플 선택바처럼, 기본 세그먼트는 옛날 것 같다"): 완전 둥근 트랙(tertiarySystemFill, 안쪽 3) 위에 **선택 칸은 검정 채움 알약 + 흰 글자**, 비선택은 회색 13pt semibold. 그림자·유리 없음 — 메일 카테고리·피트니스 기간 선택 문법. 콘텐츠 층의 세그먼트는 애플도 유리를 안 쓴다(유리는 내비게이션 층). 22:12 의 `system`(UISegmentedControl 모양: 모서리 9 트랙 + 그림자 흰 칸)은 한 시간 만에 "옛날 것 같다" 로 대체됐고 모양은 남아 있다. `glass` 모양도 남아 있다. **라이브러리의 전체·미청취·완청은 세그먼트가 아니다**(2026-09-25 PM "애플이었으면") — HIG: 세그먼트는 뷰 전환용이고 같은 목록의 콘텐츠 필터에는 쓰지 않는다. 팟캐스트·메일처럼 **필터 시트의 라디오**로 옮기고(`TopicFilterSheet` 상태 섹션), 머리 줄엔 필터 원 버튼 + 조건 요약 한 줄만 둔다(`changes/pending/library-status-filter-in-sheet.md`). `segmentHeight` prop 은 남아 있다. 밑줄 탭은 iOS 문법이 아니다 — 배타적 뷰 전환은 항상 이 부품. 흰 트랙 + 테두리, 같은 폭 칸, 선택 알약 하나(iOS 26 유리 렌즈 `GlassPill`, 그 밑은 그림자로 뜬 흰 알약)가 snappy 스프링으로 이동, 라벨 굵기는 선택과 무관하게 같다(폭이 변하면 흔들린다).

### 격자 타일 (라이브러리·탐색)
정사각 아트워크 `lg` 16 연속 곡률. 표식은 사진 위에 최소로: 우상단 더보기 = 흰 둥근 점 3개 + 드롭섀도(원 없음) · 아래 변 진행률 바 4pt(채움 `color.progress`, 완청은 100% 고정) · 탐험 편 "새로운 주제"는 아래 변 그라데이션 위 흰 글자. **완청 체크 마크는 두지 않는다.**

### 인기 대표 카드
커버가 카드의 위·양옆 변에 붙는다(카드가 클립, `lg`). 패딩은 아래·제목·알약 줄에만. **하단(제목·알약 줄) 바탕은 커버가 흐리게 이어진 면**(2026-09-25 PM "blur 처리해서 이어진 것처럼") — 카드 전체에 blurRadius 36 커버 + 차콜 45% 막, 위 정사각형은 선명한 커버가 덮는다. 글자는 플레이어 팔레트(흰색).

### 잔여 재생 표시
링 28 + 가운데 숫자(11·700). 원호 = 남은/한도(12시부터 시계 방향, 둥근 끝). 소진은 주의색.

### 검색 상자
**유리 캡슐**(`shared/ui/GlassCapsule` — GlassSurface regular + hairline, `full`), **높이 40 = `HEADER_CONTROL_HEIGHT`** — 옆의 툴바 캡슐(링 + 필터)·탐색의 링 원과 같은 높이(2026-09-25 PM "높이 맞춰"). iOS 26 시스템 검색과 같은 모양(2026-09-24 PM). 종전 `surface` 면 + `md` 12(09-22)는 폐기. 잔여 재생 링도 같은 유리 원(36, 필터 원과 같은 크기)에 담는다.

### 더보기 아이콘
`shared/ui/MoreIcon` — 글자 `⋯` 가 아니라 도형(점 r 1.8 ×3). 사진 위는 `shadow` 로 드롭섀도.

## 6. 터치·접근성 (HIG: Accessibility)

- 눌리는 영역 44×44pt 이상. 보이는 크기를 줄일 땐 `hitSlop` 으로 채운다.
- 사진 위 표식·바에는 반드시 낭독기 라벨("N% 들음"·"완청한 콘텐츠"·"새로운 주제").
- 장식 요소는 `accessibilityElementsHidden` + `importantForAccessibility="no"`.

## 7. 바꿀 때의 절차

1. HIG 에 해당 항목이 있으면 그 값·문법을 먼저 따른다. 없으면 이 문서의 결에 맞춘다.
2. 새 값은 **토큰·상수 한 곳**에 둔다(`theme`, `motion`, 컴포넌트 상단 상수). 리터럴을 흩뿌리지 않는다.
3. 실기기에서 보고 조절한다 — 값은 대부분 실물 튜닝 대상이다. 조절한 값과 이유를 코드 주석에 남긴다.
4. 이 문서를 같이 고친다. 문서와 코드가 다르면 다음 사람이 어느 쪽을 믿을지 모른다.

## 8. 미결

- 다크 모드 — 토큰만 참조하도록 강제해 두었다(convention.md). 플레이어는 이미 어둡다.
- `prefers-reduced-motion`(iOS "동작 줄이기") — 스프링·시트 모션을 페이드로 대체하는 분기가 없다.
- 플레이어 화면 안 컨트롤 영역의 유리 적용 — 흐린 커버 바탕 위라 실물로 판단한다.
- 안드로이드에서 연속 곡률·리퀴드 글라스는 없다 — 원호·블러로 폴백된다. 안드로이드 전용 재질 규칙은 정하지 않았다.

## 9. 오늘까지의 결정 이력 (2026-09-22 ~ 23)

알약 배지 → 그라데이션 글자 · 완청 체크 → 꽉 찬 바 · 재생바 색 파랑 → systemGray · 썸네일·모달·검색 상자 연속 곡률 · 아트워크 실려 내려가기 · 속도 계승 스프링 시트 · 링 잔여 표시 · 세그먼트 슬라이드 · 더보기 점 3개+드롭섀도 · 미니플레이어 카테고리 · 대표 카드 커버 블리드 · 팝업 [오늘은 그만 보기] 모서리 · 모션 곡선 통일 · 유리 미니플레이어·탭 바(rt 5) · 캡슐 탭 바 + 미니 카드. 상세는 각 PR(#596~#626)과 `docs/changes/pending/`.
