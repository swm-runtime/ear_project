/**
 * 재청취 창 — **차감이 발생한 서비스 날짜로부터 15일(당일 포함)**
 * (`paywall.md` 4.3-1 — 확정 2026-08-10).
 *
 * 한 번 차감된 콘텐츠는 이미 한도 1회를 지불한 콘텐츠이므로, 날짜가 바뀌었다고 그 소비를
 * 다시 청구하지 않는다. 4.3의 "같은 날 재청취 무료"는 이 창의 특수한 경우다.
 */
export const REPLAY_WINDOW_DAYS = 15;

/**
 * 서명 URL 만료 — **수 분 단위**(`architecture.md` 9.4). 구체 값은 서버 설정이며
 * 계약은 `expires_in_sec`으로 값에 독립적이다(`player-api.md` 4.1 · 9장).
 *
 * 짧을수록 회수 반영 지연의 상한이 짧아지지만(9.4), 재생 중 갱신 왕복이 잦아진다.
 * 10~15분짜리 에피소드 하나에 갱신이 두어 번 드는 값으로 잡았다.
 */
export const AUDIO_URL_TTL_SEC = 300;

/**
 * `listened_sec_delta` 상한 — **서버 구현 재량으로 확정된 값**(`player-api.md` 9장).
 *
 * 이 값은 파트너 정산의 재생 점유율 근거라(`domain.md` 6.3) 조작·버그로 부풀려지면
 * 배분이 왜곡된다. 정상 클라이언트의 저장 주기는 5초이고 백그라운드 복귀·오프라인 큐의
 * 누적분을 감안해도 한 번에 한 시간을 넘길 이유가 없다. 초과분은 **거부가 아니라 클램프**다
 * — 백그라운드 동기화라 사용자에게 알릴 수단이 없고, 거부하면 정상 청취분까지 유실된다.
 */
export const MAX_LISTENED_SEC_DELTA = 3600;
