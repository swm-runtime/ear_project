# architecture.md 9.5 — 모든 비밀값은 환경 변수로 주입한다. .env는 커밋하지 않는다.
# 이 파일을 .env로 복사해 로컬 값을 채운다.

NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=ear
DB_PASSWORD=change-me
DB_NAME=ear

# 쉼표로 구분한 허용 오리진 목록. `*`를 쓰지 않는다.
CORS_ORIGINS=http://localhost:8081
