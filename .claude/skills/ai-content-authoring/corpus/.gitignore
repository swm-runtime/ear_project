# 대본 원본은 커밋하지 않는다 — 남의 저작물이다.
# 유튜브 전사본·방송 대본·타사 팟캐스트 스크립트는 우리 자산이 아니며,
# 레포에 넣으면 배포 범위를 우리가 통제할 수 없다.
#
# 커밋되는 것은 여기서 뽑은 **수치**뿐이다 → ../reference/style-profile.json
# 수치는 저작물이 아니고, check.py가 실제로 쓰는 것도 그쪽이다.
#
# 원본은 각자 로컬에만 둔다. 프로파일을 다시 뽑으려면 README.md를 본다.
*.md
*.txt
*.vtt
*.srt
!README.md
