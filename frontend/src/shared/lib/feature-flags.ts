/**
 * 빌드 타임 기능 플래그 — `EXPO_PUBLIC_*` 는 번들에 인라인되므로 켜고 끄려면 새 번들(OTA 또는 빌드)이
 * 필요하다. 서버 설정처럼 나중에 바꿀 수 없다는 점을 알고 쓴다.
 */

/**
 * 구독 UI 노출 — **MVP 바이너리에서는 끈다**(KAN-66, 2026-09-17).
 *
 * App Store 심사 2.1(b): 인앱 결제 상품 없이 "구독"을 언급하면 반려된다. 앱에 결제 구현이 없으므로
 * 설정 구독 섹션·프로필 [구독 알아보기]·잔여 재생 소진 라벨의 구독 안내·탈퇴 사유 "구독 가격"을
 * 전부 **비노출**한다. 삭제가 아니다 — 결제(KAN-40 + 결제 라이브러리)가 붙으면 이 플래그만 켠다.
 *
 * 끄는 쪽이 기본값이다. 켜려면 EXPO_PUBLIC_SUBSCRIPTION_UI=on 을 명시한다 — env 누락으로
 * 구독 문구가 실린 바이너리가 심사에 나가는 사고가 없는 쪽이다.
 */
export const IS_SUBSCRIPTION_UI_ENABLED = process.env.EXPO_PUBLIC_SUBSCRIPTION_UI === 'on';
