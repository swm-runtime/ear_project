diff --git a/node_modules/react-native-screens/ios/RNSScreen.h b/node_modules/react-native-screens/ios/RNSScreen.h
index b1f482c..925a5a8 100644
--- a/node_modules/react-native-screens/ios/RNSScreen.h
+++ b/node_modules/react-native-screens/ios/RNSScreen.h
@@ -50,6 +50,8 @@ NS_ASSUME_NONNULL_BEGIN
 - (void)calculateAndNotifyHeaderHeightChangeIsModal:(BOOL)isModal;
 - (void)notifyFinishTransitioning;
 - (RNSScreenView *)screenView;
+// [ear patch] diagnostics hook (see RNSScreen.mm)
++ (void)earNoteZoomDiagnostic:(NSString *)note;
 - (void)setViewToSnapshot;
 - (CGFloat)calculateHeaderHeightIsModal:(BOOL)isModal;
 - (BOOL)isRemovedFromParent;
diff --git a/node_modules/react-native-screens/ios/RNSScreen.mm b/node_modules/react-native-screens/ios/RNSScreen.mm
index 9b66012..49d243b 100644
--- a/node_modules/react-native-screens/ios/RNSScreen.mm
+++ b/node_modules/react-native-screens/ios/RNSScreen.mm
@@ -1417,6 +1417,10 @@ Class<RCTComponentViewProtocol> RNSScreenCls(void)
   BOOL _isSwiping;
   BOOL _shouldNotify;
   BOOL _isRemovedFromParent;
+  // [ear patch] set when an interactive (system-driven) modal dismissal completed and JS was already notified
+  BOOL _notifiedInteractiveDismiss;
+  // [ear patch] the current disappearance is a system-driven (interactive) modal dismissal
+  BOOL _dismissingInteractively;
 }
 
 #pragma mark - Common
@@ -1461,9 +1465,68 @@ Class<RCTComponentViewProtocol> RNSScreenCls(void)
   }
 }
 
+// [ear patch] settle a system-driven modal dismissal exactly once: drop the zoom transition hook so UIKit lets go of
+// this controller (a dismissed zoom modal re-appeared for a frame when the source view - the tab bar accessory - was
+// re-laid out on a tab switch, 2026-09-26), clean the stack's modal bookkeeping and notify JS.
+- (void)earSettleInteractiveDismissFrom:(NSString *)origin
+{
+  if (_notifiedInteractiveDismiss) {
+    return;
+  }
+  _notifiedInteractiveDismiss = YES;
+  _dismissingInteractively = NO;
+#if __IPHONE_OS_VERSION_MAX_ALLOWED >= 180000
+  if (@available(iOS 18.0, *)) {
+    self.preferredTransition = nil;
+  }
+#endif
+  UIView *container = self.screenView.reactSuperview;
+  if ([container isKindOfClass:[RNSScreenStackView class]]) {
+    [(RNSScreenStackView *)container modalWasDismissedNatively:self];
+  }
+  [RNSScreen earNoteZoomDiagnostic:[NSString stringWithFormat:@"settled:%@", origin]];
+  [self.screenView notifyDismissedWithCount:1];
+}
+
+// [ear patch] diagnostics go to the app's ZoomTransitionRegistry (local Expo module) when present
++ (void)earNoteZoomDiagnostic:(NSString *)note
+{
+  Class registry = NSClassFromString(@"ZoomTransitionRegistry");
+  if (registry != nil && [registry respondsToSelector:@selector(noteDiagnostic:)]) {
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Warc-performSelector-leaks"
+    [registry performSelector:@selector(noteDiagnostic:) withObject:note];
+#pragma clang diagnostic pop
+  }
+}
+
 - (void)viewWillDisappear:(BOOL)animated
 {
   [super viewWillDisappear:animated];
+  // [ear patch] iOS 18 zoom transition: the user dragged/pinched the modal away. The stack never initiated this
+  // dismissal and presentationControllerDidDismiss is not called for full-screen presentations, so the stack's
+  // modal bookkeeping went stale and a later container update re-presented the screen for a frame
+  // (2026-09-26 — player flashed after switching tabs). Settle it the moment the interactive dismissal completes.
+  _dismissingInteractively = self.isBeingDismissed && self.transitionCoordinator != nil &&
+      self.transitionCoordinator.isInteractive && self.presentingViewController != nil;
+  [RNSScreen earNoteZoomDiagnostic:_dismissingInteractively ? @"willDisappear:interactive" : @"willDisappear:other"];
+  if (_dismissingInteractively) {
+    __weak RNSScreen *weakSelf = self;
+    [self.transitionCoordinator
+        animateAlongsideTransition:nil
+                        completion:^(id<UIViewControllerTransitionCoordinatorContext> context) {
+                          RNSScreen *strongSelf = weakSelf;
+                          if (strongSelf == nil) {
+                            return;
+                          }
+                          if (context.isCancelled) {
+                            strongSelf->_dismissingInteractively = NO;
+                            [RNSScreen earNoteZoomDiagnostic:@"coordinator:cancelled"];
+                            return;
+                          }
+                          [strongSelf earSettleInteractiveDismissFrom:@"coordinator"];
+                        }];
+  }
   // self.navigationController might be null when we are dismissing a modal
   if (!self.transitionCoordinator.isInteractive && self.navigationController != nil) {
     // user might have long pressed ios 14 back button item,
@@ -1517,7 +1580,23 @@ Class<RCTComponentViewProtocol> RNSScreenCls(void)
 - (void)viewDidDisappear:(BOOL)animated
 {
   [super viewDidDisappear:animated];
-  if (self.parentViewController == nil && self.presentingViewController == nil) {
+  // [ear patch] fallback for the interactive (zoom) dismissal: UIKit may still report presentingViewController here,
+  // so re-check on the next run loop turn once the presentation is fully torn down.
+  if (_dismissingInteractively && !_notifiedInteractiveDismiss) {
+    __weak RNSScreen *weakSelf = self;
+    dispatch_async(dispatch_get_main_queue(), ^{
+      RNSScreen *strongSelf = weakSelf;
+      if (strongSelf != nil && strongSelf.parentViewController == nil && strongSelf.presentingViewController == nil) {
+        [strongSelf earSettleInteractiveDismissFrom:@"didDisappear:deferred"];
+      }
+    });
+  }
+  // [ear patch] for an interactive (zoom) dismissal the stock notify below fires on the transition's last frame —
+  // React then unmounts the screen view while UIKit is still finishing the transition, and the orphaned view
+  // flashed on the next tab switch (2026-09-26 22:42 device trace: modals:n=0 arrived before settled:coordinator).
+  // Only the settle path (coordinator completion / deferred check) may notify in that case.
+  if (self.parentViewController == nil && self.presentingViewController == nil && !_notifiedInteractiveDismiss &&
+      !_dismissingInteractively) {
     if (self.screenView.preventNativeDismiss) {
       // if we want to prevent the native dismiss, we do not send dismissal event,
       // but instead call `updateContainer`, which restores the JS navigation stack
diff --git a/node_modules/react-native-screens/ios/RNSScreenStack.h b/node_modules/react-native-screens/ios/RNSScreenStack.h
index 9ae11c3..914df21 100644
--- a/node_modules/react-native-screens/ios/RNSScreenStack.h
+++ b/node_modules/react-native-screens/ios/RNSScreenStack.h
@@ -24,6 +24,9 @@ NS_ASSUME_NONNULL_BEGIN
 
 - (void)markChildUpdated;
 - (void)didUpdateChildren;
+// [ear patch] A modal owned by this stack was dismissed by the system (iOS 18 zoom transition interactive
+// dismissal) — drop it from the bookkeeping the same way presentationControllerDidDismiss does for sheets.
+- (void)modalWasDismissedNatively:(UIViewController *)modal;
 - (void)startScreenTransition;
 - (void)updateScreenTransition:(double)progress;
 - (void)finishScreenTransition:(BOOL)canceled;
diff --git a/node_modules/react-native-screens/ios/RNSScreenStack.mm b/node_modules/react-native-screens/ios/RNSScreenStack.mm
index 192f5d2..9046b7e 100644
--- a/node_modules/react-native-screens/ios/RNSScreenStack.mm
+++ b/node_modules/react-native-screens/ios/RNSScreenStack.mm
@@ -394,6 +394,16 @@ RNS_IGNORE_SUPER_CALL_END
 
   _updatingModals = YES;
 
+  // [ear patch] diagnostics — what the stack sees when modals get reconciled (a dismissed zoom modal re-appeared
+  // for a frame on a tab switch, 2026-09-26). Read from the app via ZoomTransitionRegistry.noteDiagnostic.
+  {
+    UIViewController *pvc = _controller.presentedViewController;
+    [RNSScreen earNoteZoomDiagnostic:[NSString stringWithFormat:@"modals:n=%lu pm=%lu pvc=%@",
+                                                                (unsigned long)controllers.count,
+                                                                (unsigned long)_presentedModals.count,
+                                                                pvc == nil ? @"nil" : NSStringFromClass([pvc class])]];
+  }
+
   // We need to find bottom-most view controller that should stay on the stack
   // for the duration of transition.
 
@@ -481,6 +491,49 @@ RNS_IGNORE_SUPER_CALL_END
         return;
       }
 
+      // [ear patch] iOS 18 zoom transition — if the app registered a source view (local Expo module
+      // `modules/zoom-transition`, class ZoomTransitionRegistry), the modal zooms out of that view and
+      // shrinks back into it on dismiss (Apple Music mini player -> Now Playing). Looked up by name so
+      // builds without the module keep the stock behaviour.
+#if __IPHONE_OS_VERSION_MAX_ALLOWED >= 180000
+      if (@available(iOS 18.0, *)) {
+        if (shouldAnimate) {
+          Class registry = NSClassFromString(@"ZoomTransitionRegistry");
+          if (registry != nil && [registry respondsToSelector:@selector(takeSourceView)]) {
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Warc-performSelector-leaks"
+            UIView *zoomSource = [registry performSelector:@selector(takeSourceView)];
+#pragma clang diagnostic pop
+            if (zoomSource != nil) {
+              __weak UIView *weakZoomSource = zoomSource;
+              UIZoomTransitionOptions *zoomOptions = [UIZoomTransitionOptions new];
+              // The app blocks the system drag/pinch dismissal while a panel is open or the touch started on a
+              // scrolling list (ZoomTransitionRegistry.isInteractiveDismissBlocked, NSNumber for performSelector).
+              zoomOptions.interactiveDismissShouldBegin = ^BOOL(UIZoomTransitionInteractionContext *_Nonnull context) {
+                Class blockedRegistry = NSClassFromString(@"ZoomTransitionRegistry");
+                if (blockedRegistry == nil ||
+                    ![blockedRegistry respondsToSelector:@selector(isInteractiveDismissBlocked)]) {
+                  return YES;
+                }
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Warc-performSelector-leaks"
+                NSNumber *blocked = [blockedRegistry performSelector:@selector(isInteractiveDismissBlocked)];
+#pragma clang diagnostic pop
+                return ![blocked boolValue];
+              };
+              next.preferredTransition = [UIViewControllerTransition
+                  zoomWithOptions:zoomOptions
+                sourceViewProvider:^UIView *_Nullable(UIZoomTransitionSourceViewProviderContext *_Nonnull context) {
+                  return weakZoomSource;
+                }];
+            }
+          }
+        }
+      }
+#endif
+
+      [RNSScreen earNoteZoomDiagnostic:[NSString stringWithFormat:@"present:%@ anim=%d",
+                                                                  NSStringFromClass([next class]), shouldAnimate]];
       [previous presentViewController:next
                              animated:shouldAnimate
                            completion:^{
@@ -525,6 +578,9 @@ RNS_IGNORE_SUPER_CALL_END
         // See: https://github.com/software-mansion/react-native-screens/issues/2048
         // For now, to mitigate the issue, we also decide to trigger its dismissal before
         // starting the presentation chain down below in finish() callback.
+        [RNSScreen earNoteZoomDiagnostic:[NSString stringWithFormat:@"dismiss:%@ beingDismissed=%d",
+                                                                    NSStringFromClass([firstModalToBeDismissed class]),
+                                                                    firstModalToBeDismissed.isBeingDismissed]];
         if (!firstModalToBeDismissed.isBeingDismissed) {
           // If the modal is owned we let it control whether the dismissal is animated or not. For foreign controllers
           // we just assume animation.
@@ -1001,6 +1057,17 @@ RNS_IGNORE_SUPER_CALL_END
   [self updateContainer];
 }
 
+// [ear patch] see RNSScreenStack.h
+- (void)modalWasDismissedNatively:(UIViewController *)modal
+{
+  if ([_presentedModals containsObject:modal]) {
+    [RNSScreenWindowTraits updateWindowTraits];
+    [_presentedModals removeObject:modal];
+    _updatingModals = NO;
+    [self emitOnFinishTransitioningEvent];
+  }
+}
+
 - (void)didUpdateChildren
 {
   // do nothing
